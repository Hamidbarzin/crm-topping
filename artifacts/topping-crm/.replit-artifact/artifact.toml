kind = "web"
previewPath = "/"
title = "Topping CRM"
version = "1.0.0"
id = "artifacts/topping-crm"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20411

[services.development]
run = "pnpm --filter @workspace/topping-crm run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/topping-crm", "run", "build" ]
publicDir = "artifacts/topping-crm/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20411"
BASE_PATH = "/"
